# Foundation records

Governing brief and accepted foundational choices. Use `maintain.py record list --topic foundation` for the generated chronological inventory, then `record read --id ID --history` for exact history. See the [record-store guide](../README.md); [current state](../../current-state.md) owns current status and permission.
