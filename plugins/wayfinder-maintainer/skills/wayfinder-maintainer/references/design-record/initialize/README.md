# Initialize records

Initialize policy and implementation/certification history. `policy/` contains undivided policy context; `candidate-revision-N/` groups implementation records by candidate. Use `maintain.py record list --topic initialize`, then `record read --id ID --history`. See the [record-store guide](../README.md); [current state](../../current-state.md) owns current status and permission.
