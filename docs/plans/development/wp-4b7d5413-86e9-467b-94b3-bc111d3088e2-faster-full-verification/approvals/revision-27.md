<!-- WAYFINDER-PLAN:BEGIN -->
```json
{"approval":{"locator":"conversation:2026-09-15/dotnet-prototype/revision-26-approval","quotation":"Yes"},"approvalHistory":{"13":"7ba91b47b3d7de807ef029bb53a8dc7018b47592d49e7ebb71efa98f38a1b618","2":"a77c853e193ceb7867c14e66d36f1f474ae922edb304c364ddd9598a3459513b","20":"acd35dc3dc013611e1d0cf8b39aff9ce7811391785e307a0c9cf0feb76622a33","23":"fffc2342077b2549322670bd61a1bec786fb46346af8beaaad72dc44ddcfef15","9":"6b717fb40fdbaad10f8545712a4fd97598e639366fbd3e550097ce99d7e6ea48"},"approvedRevision":27,"approvedSha256":null,"format":"wayfinder-plan","id":"wp-4b7d5413-86e9-467b-94b3-bc111d3088e2","records":[],"revision":27,"schemaVersion":1,"slug":"faster-full-verification","status":"approved","subject":"development","summary":"Preserve full PR conformance coverage with bounded process-isolated case execution and a ten-minute measurement checkpoint.","title":"Faster full repository verification","updated":"2026-09-15"}
```
<!-- WAYFINDER-PLAN:END -->

# Faster full repository verification

## Summary

Keep all 305 cases for every registered adapter on each PR. Add bounded, process-isolated case execution to ordinary verification, targeting **under 10 minutes for the complete validation job** on the existing Ubuntu runner.

Current evidence establishes serial execution and two 30-minute timeouts during conformance. PowerShell startup is a suspected bottleneck; its contribution remains unmeasured because PowerShell is unavailable locally.

## Implementation

- Add `--jobs N` to `maintain.py test` and the conformance runner, defaulting to `1`. Reject non-positive values before execution.
- Use a spawn-based standard-library process pool for parallel runs. Each task executes one complete case with its existing temporary workspace and fresh adapter subprocesses.
- Keep invocations within each case sequential. Reset case-local observation state between tasks; emit results centrally in original suite order.
- Preserve result schemas, case selection, diagnostics, the 15-second invocation timeout, and nonzero failure behavior. Worker crashes or missing results must fail the run.
- Keep evidence, parity, and certification paths serial. Reject parallel execution with evidence writing or observation collection in this tranche.
- Update ordinary validation to opt into workers for PowerShell only. Retain the current workflow job, other adapter execution, runtime pins, installation checksums, and 30-minute timeout.

## Measurement checkpoint

- Measure setup, doctor, self-test, each adapter suite, and total job duration. Collect case durations and adapter invocation counts separately from governed results.
- Compare full serial PowerShell execution with `--jobs 2` and `--jobs 4` on the same runner class and source revision.
- Choose the smallest worker count that achieves the target, then require three consecutive complete validation runs under 10 minutes with full passing coverage.
- **If neither configuration meets the target, pause and revise the plan with measurements.** Job sharding, reduced coverage, larger runners, and adapter changes are not automatic fallbacks.

## Verification and delivery

- Test serial compatibility, filtered selection, ordered aggregation, workspace isolation, ordinary case failures, subprocess timeouts, worker crashes, and invalid worker counts.
- Compare serial and parallel result sets for all 305 PowerShell cases; require identical coverage and passing outcomes.
- Run canonical maintainer self-tests, repository validation, focused conformance, and pre/post doctor checks. Report local PowerShell coverage as unavailable; require hosted PowerShell verification before review readiness.
- Deliver one PR against `candidate-revision-9-certification`, containing the harness change, regression tests, workflow opt-in, documentation, and timing findings. Stop for version-bound owner review.

## Authority and assumptions

- This is a planning proposal; no repository files or durable plan records have been written in Plan Mode.
- After leaving Plan Mode, persist the complete draft through the maintainer plan CLI and obtain explicit approval before implementation or CI execution.
- The intended approval covers plan persistence, isolated branch work, commits, push, PR creation, and ordinary validation needed for the benchmark.
- Preserve existing local edits, frozen contract and adapter bytes, accepted history and evidence, and current candidate status. Exclude dependency additions, certification dispatch, evidence publication, activation, settings changes, and integration into `main`.

## Execution progress

The owner explicitly approved this complete plan with "PLEASE IMPLEMENT THIS PLAN:" on 2026-09-15. Exact approved snapshot revision 2 remains unchanged. Implementation is on codex/faster-full-verification in an isolated worktree based on cca387cdf63cc4784f6bf2a543b67f17f484e8e8, targeting candidate-revision-9-certification. The owner checkout retains its pre-existing local changes and separately persists approval record wr-0051; unrelated unpublished closure history is excluded from this PR.

Whole-case spawn workers, positive jobs validation, ordered fail-closed aggregation, separate case/invocation timings, ordinary verification timing wrapper, regressions and documentation are implemented. The temporary ordinary CI benchmark switch selects 1/2/4 workers on rerun attempts 1/2/3 of the same revision; it will be removed in favor of the selected fixed count before readiness. No certification/publication dispatch or artifact download is authorized or performed.

Canonical self-test: 123 test cases, 122 passed, one genuine missing-PowerShell skip, zero bytecode. Repository validation and whitespace checks pass. Full local Python serial and two-worker runs each passed all 305 cases with identical complete result data; Node serial also passed all 305. Python timings were 168.766 seconds serial and 90.020 seconds with two workers; Node was 68.370 seconds. Pre-edit doctor passes 35/36 with missing pwsh as the sole unchanged failure. CPython is 3.12.14 and Node.js is 22.22.3 locally; pinned hosted versions remain required. PowerShell coverage and hosted timing checkpoint are pending and block review readiness. No implementation acceptance or merge is claimed.

The owner instructed "We should make it explicit in the wayfinder-maintainer skill that PRs use the PR template". The skill and PR guidance now require reading the target template, preserving its sections and checklists, and checking only verified passing items. Draft PR #9 was corrected to the repository template. This reinforces delivery expectations without changing the approved performance acceptance criteria.

Hosted ordinary validation run 34987046636 attempt 1 passed all 305 cases for each adapter on the pinned Ubuntu runner. Complete job duration was 1775 seconds (29m35s). Diagnostic setup was 9.190 seconds; repository validation 0.601, doctor 5.478, self-test 33.015, Python 351.403, Node 50.605, and serial PowerShell 1318.375 seconds. Each adapter suite recorded 900 invocations. Attempt 2 passed all 305 cases per adapter on the same source revision with two PowerShell workers. The full job took 923 seconds (15m23s), missing the ten-minute target; PowerShell took 567.994 seconds, Python 270.710, Node 41.411, self-test 24.974, doctor 4.939, validation 0.326 and diagnostic setup 8.236 seconds. Attempt 3 passed all 305 cases per adapter on the same revision with four PowerShell workers. Complete job duration was 858 seconds (14m18s), also missing the ten-minute target. PowerShell took 508.366 seconds; Python 267.096, Node 39.780, self-test 25.259, doctor 4.272, validation 0.409 and diagnostic setup 8.270 seconds. Four-worker comparison and three consecutive fixed-configuration runs remain pending; review readiness remains blocked. These diagnostic timings are not accepted certification evidence.

## Measurement checkpoint: paused for revision

All three benchmark attempts used Ubuntu 24.04, PR head 28b5d75637d6f30344febb8454de793089e6b07e and the same tested merge revision ce65e3733c309d4d0edc458d1fed24074f9e5de6. Full serial and parallel result data were each checked against the exact ordered 305-case oracle (IDs, categories, rules and passing status), establishing identical coverage and passing outcomes. Every adapter suite recorded 900 invocations. Hosted doctor passed 36/36; local PowerShell remains explicitly unavailable.

Neither two nor four workers achieves the target. The approved measurement checkpoint therefore stops affected implementation and CI execution pending an explicitly approved revised plan. No worker count is selected, no three-run readiness validation is started, and draft PR #9 is not ready for owner review or merging. Existing temporary benchmark switch remains on the published head; local PR-template guidance commit 3a7f43c is not yet pushed, avoiding another automatic validation run while paused.

The next bounded activity is a facilitated plan revision using these measurements. The ten-minute acceptance target remains unchanged until an explicit owner decision. Job sharding, reduced coverage, larger runners, adapter changes and parallelizing other adapters remain outside current implementation authority. No fallback or implementation acceptance is inferred.

## Proposed bounded diagnostic revision

The owner instructed "Let's do it" in response to comparing empty pwsh, adapter invalid-command rejection and real commands on the same runner. This authorizes concrete diagnostic preparation. Because the approved performance checkpoint is paused, the complete revision below awaits explicit approval before commit/push or hosted execution. This does not authorize parallel Python or another performance fallback.

- Add scripts/measure_powershell_startup.py, maintainer regression coverage, diagnostic documentation, and a temporary diagnostic step in the existing validate job on Ubuntu 24.04. Preserve runtime pins, installer checksum, current job and 30-minute timeout. No dependency, adapter, contract or accepted evidence changes.
- Run empty pwsh -NoLogo -NoProfile -NonInteractive -Command 'exit 0'; the unchanged adapter with invalid command initialize-activate (expected exit 2, command.unknown); probe; and discover with a temporary valid manifest fixture. Validate each expected exit, JSON envelope and stderr behavior.
- Record first observations separately, then 16 fresh-process samples per scenario at one/two/four workers: 196 invocations. Fixture creation is outside measured regions. Bound invocation timeout to 15 seconds and the hosted diagnostic step to five minutes. Fail closed on unexpected results, timeouts, existing reports or modified adapter bytes.
- Collect per-invocation wall time, median/p95, batch wall time, aggregate child user/system CPU, major page faults, block I/O counts and context switches. CPU counters are per group, not per parallel invocation. Record CPU count, platform, adapter SHA-256 and source/run binding. First observations are not guaranteed cold-cache samples; differences are estimates, not proof of individual startup components. No cache clearing, settings change or profiler/dependency installation.
- Keep diagnostic JSON/logs separate from governed conformance and accepted evidence. No Actions artifact downloads or evidence promotion/publication.
- Remove the completed attempt-number benchmark switch; accompany diagnostics with ordinary verification using two PowerShell workers. Retain serial Python/Node and all 305 cases per registered adapter. Run full ordinary verification even if the diagnostic step fails. Two workers are a checkpoint configuration, not final selection, and the full-job ten-minute target remains unchanged.
- Approval of this complete revision permits local completion/tests, commits and non-force push of this isolated branch (including already-prepared template guidance and findings), update to draft PR #9 using its template, exactly one automatic ordinary validation attempt with diagnostics, read-only metadata/log inspection, and local measurement/progress/current-state persistence. Additional attempts need an explicit decision if the diagnostic fails. No readiness run series, merging or owner PR-review handoff is implied.
- Run canonical self-test, repository validation, focused Python conformance and pre/post doctor; state local PowerShell unavailable. Hosted measurements supply required diagnostic coverage. Success means all 196 measurements complete with expected outcomes, unchanged adapter bytes and retained full ordinary coverage; report any failures separately. Do not infer that diagnostic success meets performance acceptance.
- After reading and persisting findings, pause for one measured plan-revision decision in chat. Further performance implementation, other adapter parallelism, sharding, reduced coverage, larger runners and adapter changes remain excluded until the owner approves a complete next revision. Existing certification/publication/activation/settings/main exclusions continue.

### Diagnostic preparation verification

The script, regression tests, workflow step and diagnostic documentation are prepared locally and uncommitted. Canonical self-test on final code: 127 test cases, 126 passed, one genuine local PowerShell-unavailable skip, zero failures/errors and bytecode. Repository validation and git diff --check passed. Focused Python command-unknown, command-envelope and discover-explicit conformance passed 3/3. Pre/post doctor retains only the unchanged unavailable pwsh probe (35/36). No hosted diagnostics, commit or push of this revision has occurred. These checks establish local harness preparation; actual PowerShell diagnostics remain required hosted coverage.

## Approved diagnostic execution progress

The owner answered "yes, I approve" to complete diagnostic revision 8 (isolated SHA-256 22335267a8c569ebb6da5e784e0d98d83cb7f66f885a4c4e718be2768b69ce66), preserved as isolated approval snapshot revision 9 and owner-checkout mirror revision 6. Durable receipt wr-0053 stays in the original checkout, excluded from this PR together with unrelated unpublished history. The prepared diagnostics, regression tests, explicit template guidance, prior timing findings and approval snapshot were committed and non-force pushed as 56907814b177425ce5b2b0f06a3ed10c1c29105b to codex/faster-full-verification. Draft PR #9 now uses the repository template and identifies both approval scopes. Exactly one ordinary validation run 35009583443 attempt 1 has started on that head; diagnostic results and complete ordinary coverage remain pending. No additional attempt or further optimization is authorized. Findings/progress persistence will remain local to avoid another automatic CI run; then pause for the next measured decision.

## Diagnostic checkpoint completed; further work paused

Exactly one approved ordinary run 35009583443 attempt 1 completed successfully on PR head 56907814b177425ce5b2b0f06a3ed10c1c29105b and tested merge revision a57242cf8ec90de62a6b5b0941fb01cc9edbf28b. Full job duration was 1347 seconds (22m27s), including the 119-second diagnostic step. Full log SHA-256 is d15e161873c2a625b49e4a9198468d77df4ceb09e7acccc820c979759da7a339. All 196 diagnostic observations had expected outcomes; first observations and each 16-sample group were reconciled from the complete log. Adapter bytes were unchanged. All 305 conformance cases passed for each adapter, each suite recorded 900 invocations, hosted doctor passed 36/36, and canonical self-test passed 127/127 with zero skips and bytecode.

Diagnostic medians (one/two/four workers): empty pwsh 0.163/0.218/0.383 seconds; adapter invalid-command rejection 0.892/1.030/1.652; probe 1.222/1.519/2.439; discovery 1.111/1.352/2.173. Bare startup is approximately 13% of the serial probe median. Rejection adds about 0.729 seconds beyond an empty process and already accounts for about 73% of probe and 80% of discovery time; parsing/loading, initialization, rejection and result formatting are not separately isolated. The leading hypothesis shifts toward repeated adapter loading/initialization and baseline execution overhead. This is a cross-scenario estimate, not a claim of exact component attribution.

Four-worker probe used 38.306 child CPU seconds in 10.022 wall seconds (3.822 cores); median latency doubled and involuntary context switches increased from 2,366 to 37,340. Discovery likewise averaged 3.818 cores. This supports CPU contention behind diminishing worker benefits. Input blocks were zero in repeated groups; output blocks were nonzero. No specific memory/disk/cache cause is established.

Ordinary check timings: validation 0.307 seconds, doctor 4.717, self-test 32.498, Python 344.497, Node 49.312 and two-worker PowerShell 786.580. Driver precheck/setup diagnostic was 126.589 seconds and includes the 119-second diagnostic step; it is not solely installation/setup time. Cross-run variation is not assigned a specific cause. This successful diagnostic run does not meet the ten-minute performance target.

The approved diagnostic operation is finished. Further CI, pushes and affected performance implementation are paused pending a complete next revision and explicit approval. No final worker count or three-run readiness series is selected; draft PR #9 remains not ready for owner PR review or merging. Findings and current progress remain local to avoid another CI attempt. Local PowerShell remains unavailable. No adapter/frozen bytes, accepted evidence, certification, publication, activation, dependencies, settings or main integration changed. The next bounded activity is one measured plan-revision decision in chat; no further diagnostic or optimization executes automatically.

## Proposed component diagnostic revision

The owner answered "Yes" to focusing the next revision on script parsing/loading, initialization and result-formatting costs. This establishes diagnostic scope and authorizes preparation of the complete proposal. It does not approve a yet-unseen complete version, a new hosted run, further performance implementation or adapter reopening. The prior diagnostic findings remain recorded and the ten-minute target remains unmet.

### Concrete probe design

Add a Python driver scripts/measure_powershell_components.py and an independent diagnostic PowerShell wrapper scripts/measure_powershell_components.ps1. The wrapper reads the unchanged registered adapter as source data; it never writes or replaces an adapter, publishes an adapter variant, or presents derived component execution as conformance. Use PowerShell's standard Parser.ParseInput source/fileName overload and ScriptBlockAst.GetScriptBlock, documented at https://learn.microsoft.com/en-us/dotnet/api/system.management.automation.language.parser.parseinput and https://learn.microsoft.com/en-us/dotnet/api/system.management.automation.language.scriptblockast.getscriptblock. No dependency, profiler installation or settings/cache change.

- Keep all component runs serial: concurrency contention has already been measured. Measure empty pwsh -NoLogo -NoProfile -NonInteractive -Command 'exit 0' as an external wall/child-CPU baseline. Two component scenarios use the same flags and run the independent wrapper in a fresh process: a minimal successful command-result payload and a real probe-result payload.
- For each scenario, record one first observation separately, then eight additional fresh-process samples. Three scenarios yield 27 process invocations total. First observations are not guaranteed cold-cache samples. Keep the existing 15-second invocation timeout and five-minute diagnostic-step cap; failures stop measurement and are reported without another attempt.
- Inside each component process, use Stopwatch to time source byte read/UTF-8 decode, full adapter parsing, creation of the diagnostic script block, its loading/top-level initialization, optional actual Invoke-WfProbe work, first ConvertTo-WfDisplayJson call, and three subsequent formatter calls separately. Formatter calls operate on the same payload in one process to expose first-use versus steady-state costs; this diagnostic does not reuse a process for conformance adapter invocations. Parent wall time and aggregate child CPU include wrapper overhead and are reported separately. Do not subtract them into an exact startup partition.
- Parse the complete original source with its original fileName; require no parse errors, no unexpected begin/process/parameter/using blocks, and exact final two top-level statements Invoke-WfMain $args and exit $script:ExitCode. Build only an in-memory initialization prefix before those statements and parse it with the original source path. Time this extra diagnostic-prefix parse as preparation, not as part of ordinary adapter startup. GetScriptBlock creation is not claimed to include all compilation/JIT; the load/initialization interval may include deferred compilation.
- Dot-source the diagnostic script block solely in its fresh wrapper process. Check its source-path metadata and resulting SkillRoot against the exact original skill root before invoking any probe. Fail closed if those assumptions are unsupported on pinned PowerShell; no fallback to a modified adapter file, new path, scope override or rewritten source. The original adapter bytes and registry entry remain untouched.
- Inventory loaded module names/versions before and after loading outside timed regions, using core Get-Module only; do not pre-load management/utility modules to help the adapter. Record relevant source offset/length, source SHA-256, exact runtime version, run/source identity and timer labels. Instrumentation can change cache/JIT state and derived script-block invocation differs from -File; state those limitations explicitly.
- Minimal payload has the existing format/schemaVersion/ok/command/code/data/diagnostics keys with successful probe command, empty data and diagnostics. Probe payload uses the existing Invoke-WfProbe output under the checked original skill root and unchanged adapter registry hash. Do not call Write-WfResult during timed formatter intervals or allow component output onto governed result channels. Measure string formatting first; separately time writing the resulting UTF-8 bytes to a private temporary file, compare read-back bytes, and remove only that owned fixture after measurement. This approximates file output, not the real stdout pipe; do not claim its time is stdout latency.
- Emit one diagnostic JSON line per process, containing component times, module inventories, payload/formatted text and source binding. The Python parent validates process exit, exact report shape, finite nonnegative times, supported timer labels, sample counts, source/adapter SHA-256, payload envelope/adapter identity, identical first/repeated formatted text and temporary output byte equality. Store exclusively created diagnostic reports separately from governed results and accepted evidence; interrupted/invalid reports remain incomplete. Check original adapter bytes unchanged after all measurements.

### Verification, hosted scope and stopping rule

Before hosted execution, implement meaningful maintainer regressions for prefix boundaries/failure on unexpected source shape, component report validation, inconsistent formatter bytes, invalid/negative/nonfinite timings, subprocess timeouts and refusal to overwrite reports. Run canonical self-test, repository validation, focused Python conformance and pre/post doctor; local PowerShell remains explicitly unavailable. Hosted coverage must exercise the actual wrapper, including original-source path/scoping assumptions and formatter validation. Do not invent local PowerShell passes.

Replace the completed startup diagnostic workflow step with this component step in the current Ubuntu 24.04 validate job, preserving pinned runtimes, installation checksum, 30-minute timeout and all 305 cases per adapter. Ordinary verification still uses two PowerShell workers with serial Python/Node and runs even if diagnostics fail; this is a diagnostic accompaniment, not a final worker selection or performance readiness claim. The earlier 196-observation report and findings remain intact.

Approval of this complete revision permits continuous local implementation/testing, commits/non-force push of the isolated codex/faster-full-verification branch including already-saved diagnostic findings and plan/routing updates, a template-based draft PR #9 update, exactly one automatic ordinary full validation attempt with these 27 component observations, read-only metadata/log inspection and local findings/record/plan/current-state persistence. Then pause for one measured revision decision in chat. No additional attempt is authorized after failure without a new explicit decision, no readiness series/PR review or merging is implied, and further optimization/adapter changes remain excluded. Preserve frozen contract/adapter bytes, accepted history/evidence, activation/candidate/publication state and existing original-checkout edits. Exclude dependencies, certification/publication dispatch, evidence publication/artifact downloads, larger runners, sharding, reduced coverage, other adapter parallelism, settings/authentication changes and main integration.

Success requires all 27 expected component observations, valid report/source bindings, no unchanged-source or scope assumptions violated, verified formatted/output bytes, and full passing ordinary coverage. Report failures separately and never infer the ten-minute target was achieved from diagnostic success. The next bounded activity after findings is the single measured plan-revision question; do not begin further work automatically.

## Component implementation progress

The independent wrapper and Python driver are implemented in the isolated worktree. Frozen adapter bytes are unchanged. Canonical self-test ran 135 cases: 133 passed, two genuine PowerShell-unavailable skips, zero failures and bytecode. Repository validation and whitespace checks passed; focused Python command-unknown, discover-explicit and command-envelope passed 3/3. Both canonical doctors remain 35/36 solely because local PowerShell is unavailable. The actual wrapper AST guard and component execution require the single authorized hosted attempt. No hosted observation or performance outcome is claimed yet.

## Owner-authorized corrected replacement attempt

Component run 35014663010 attempt 1 on head c3fb70cb161f37b4f4384d83bf56b5ee0e7991ad failed before component observations: strict mode rejects accessing Count on a null NamedBlockAst.Traps collection. Nine empty-process observations passed (median 0.161560 seconds), with zero valid component observations. The AST regression omitted strict mode and missed this harness bug. Hosted doctor and self-test passed 36/36 and 135/135; full adapter coverage remains incomplete and is not passed.

The owner explicitly instructed "Let’s cancel the current attempt and push the correction to start a new attempt". This later named bounded authority supersedes the one-attempt stop only for cancelling that run and committing/non-force pushing the two-line null-safe trap guard and strict-mode regression to start one replacement automatic full validation on PR #9. Normal cancellation did not stop the always-running ordinary step; force-cancellation was requested and terminal cancellation confirmed before the replacement push. The complete approved diagnostic design, 27 serial observations, all 305 cases per adapter, pins/checksum, serial Python/Node, two ordinary PowerShell workers and timeouts remain unchanged. Corrected local self-test is 135 cases, 133 passes, two genuine PowerShell-unavailable skips, zero failures/bytecode; repository validation and whitespace pass. After the replacement, inspect logs and persist findings locally, then pause for the single measured revision decision. No further attempt, optimization, adapter/frozen/evidence changes, certification/publication/activation/settings/authentication/dependencies or merging/main integration is authorized. The original checkout preserves the separate accepted operation receipt and unpublished history remains excluded from the PR.

Cancellation is confirmed: run 35014663010 attempt 1 and validate job 104534943282 are completed/cancelled, 19:36:49–19:48:24 UTC (11m35s, incomplete rather than a full validation measurement). Terminal log SHA-256 c41009c757db2915c9affe239250eee1c5f5aef06ffd97e32af4db6580f703d5. Python and Node passed 305/305, 900 invocations each (368.274s and 50.959s); PowerShell coverage and total job verification were interrupted and are unavailable. This does not change the prior ten-minute misses. The approved two-line correction and this accurate cancellation progress are ready for the owner-authorized replacement push.

The corrected replacement is started: commit d5b5bbc4115c1592ad46a520425aa5190b520dd7 was non-force pushed to the approved branch; PR #9 template description is updated and draft status retained. Ordinary run 35016030202 attempt 1, job 104539566230, is in progress. Component measurements and complete ordinary results are pending; no pass is claimed. After completion inspect logs and persist findings locally, then pause. No further push/run is authorized automatically.

## Completed component diagnostic findings

[Replacement run 35016030202 attempt 1](https://github.com/somacdivad/wayfinder/actions/runs/35016030202/attempts/1) passed on head d5b5bbc4115c1592ad46a520425aa5190b520dd7, tested merge 999aec0fb691c552ffe3dc8b721f05b4c038ba0d. Job 104539566230 ran 19:50:37–20:11:42 UTC: 21m05s. All 27 expected observations were reconciled with exact source/path/runtime bindings, finite nonnegative timings, identical first/repeated formatter text, output byte equality and unchanged frozen adapter bytes. Hosted doctor passed 36/36, canonical self-test 135/135 with zero skips/bytecode, and all 305 cases per adapter passed with 900 invocations each. Complete log SHA-256 f5f5e0821fd8cef4f0f90ac0d8196a63ccced9c1f33c34347650658c5cbf5485. No Actions artifacts were downloaded and none of these diagnostics are accepted certification evidence.

| Median component | Minimal payload | Real probe payload |
| --- | ---: | ---: |
| External process wall | 1.108104s | 1.438942s |
| Source read/decode | 0.006436s | 0.006390s |
| Full source parse | 0.066464s | 0.064836s |
| Extra diagnostic-prefix parse | 0.030864s | 0.029909s |
| Script-block creation | 0.000985s | 0.000961s |
| Loading/initialization | 0.595564s | 0.585764s |
| Probe work | 0 | 0.332729s |
| First display formatter | 0.025961s | 0.017213s |
| Three repeated formatter calls | 0.002779/0.003370/0.002445s | 0.015480/0.007823/0.007574s |
| Private UTF-8 file write | 0.005767s | 0.005017s |

Empty PowerShell median was 0.169829s. Loading/initialization is the largest observed component, roughly nine times full-source parsing in these probes. Probe work is substantial; display formatting and file output are smaller. All 18 component observations had no loaded modules before initialization and Microsoft.PowerShell.Management 7.0.0.0 afterward. Module autoloading is a specific lead inside the loading/initialization interval, but its individual cost versus remaining execution/deferred compilation is not isolated. Do not claim that module import alone causes the full overhead. The wrapper/in-memory prefix changes compilation/cache state, prefix parsing is diagnostic overhead, formatter first-use follows earlier probe work in that scenario, and file output is not stdout latency; these medians are not an exact ordinary-startup decomposition.

Ordinary timings: repository 0.339s, doctor 4.564s, self-test 35.083s, Python 349.863s, Node 50.260s, PowerShell with two workers 789.428s. Setup/precheck 32.682s includes component diagnostics; it is not solely installation setup. Checks total 1229.544s and wrapper total 1262.227s. The full job remains above ten minutes; no final worker count, three-run readiness series, target waiver or owner PR readiness is established.

This bounded diagnostic operation is complete. Findings/progress remain local to avoid triggering another run. Draft PR #9 remains not ready for owner review/merging. No further push, CI, diagnosis or optimization is authorized automatically. Next is one measured revision-scope decision in chat; a complete replacement plan requires explicit approval before implementation/execution. The suggested diagnostic scope is separating Management module autoload from remaining initialization/deferred compilation, preserving adapter/frozen bytes. Local PowerShell remains unavailable; existing candidate/evidence/publication/activation facts and all exclusions are unchanged.

## Proposed Management module isolation revision

The owner instructed "Do it" after the measured proposal to isolate Management module autoloading from remaining initialization. This authorizes preparing this complete diagnostic revision; complete-version approval remains required before implementation or another hosted run. Earlier diagnostic findings, cancelled-attempt status, approval snapshots and ten-minute misses remain intact.

### Probe design and interpretation

Extend the independent scripts/measure_powershell_components.ps1 wrapper and scripts/measure_powershell_components.py driver with natural, preloaded and import-only Management modes. Preserve the existing strict-mode/null-safe AST boundary guard, in-memory initialization prefix, original source-path/SkillRoot checks and unchanged registered adapter bytes. No edited adapter file, adapter variant publication, process reuse in conformance, module installation, preference/profile/environment settings change or dependency.

Use the installed Microsoft.PowerShell.Management module only. Explicit Import-Module is a core command and adds the module to the current process; Get-Module inventory does not autoload modules. Primary references: https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/import-module and https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/get-module . Do not use Get-Command/Get-Help or management commands before the intended import/load intervals.

Run six scenarios, all serial and with a fresh pwsh -NoLogo -NoProfile -NonInteractive process for each observation:

1. Empty process baseline, exit 0.
2. Import-only control: time explicit Import-Module Microsoft.PowerShell.Management -ErrorAction Stop independently. Then separately time the same two Split-Path parent operations and Join-Path contract-root setup used at the adapter's top level. Compute the adapter directory/expected SkillRoot using .NET before those timed commands, assert identical paths afterward, and report module identities. Read/hash the unchanged adapter for source binding, but do not parse or execute its prefix in this control. Its report explicitly labels stages performed and does not invent zero-duration parser/formatter observations.
3. Minimal successful payload with natural autoloading during prefix initialization.
4. Identical minimal payload with Management explicitly imported immediately before prefix initialization, after source parsing and script-block creation. Time that import separately; then time initialization with the module already present.
5. Existing real probe payload with natural autoloading.
6. Identical real probe payload with the same explicit pre-import and separately timed remaining initialization.

Record one first observation for each scenario separately, then eight repeated observations per scenario: 54 fresh process invocations total. Run repeated observations as eight serial rounds with a deterministic alternating/reversed scenario order to reduce systematic ordering drift; retain each observation's round/order. Every observation still uses a fresh process; first observations are not guaranteed cold-cache samples. Keep the 15-second invocation timeout and five-minute diagnostic step cap. Stop measurements on any failure; do not trigger a retry automatically.

For component scenarios retain separately timed source read/decode, full-source parse, extra diagnostic-prefix parse, script-block creation, loading/initialization, optional probe work, first/three repeated display formatter calls and private UTF-8 file output. Natural mode has no explicit-import observation; preloaded mode records explicit import and remaining loading independently. Retain parent whole-process wall and aggregate child CPU. Inventory loaded module names/versions outside timed intervals before import/loading, after explicit import where applicable and after initialization. Use Get-Module from core only; require Management absent before and present as the installed 7.0.0.0 module afterward, with pinned PowerShell 7.6.6. Fail on unexpected source/path/runtime/module bindings. Do not preload Utility/other modules to help the target.

Define closed, versioned diagnostic report shapes for component observations and import-only controls, keeping them distinct from governed result envelopes. Exclusively create the aggregate report and leave it incomplete on interruption/failure. Validate exact scenario/mode/sample/round bindings, finite nonnegative timings, source/adapter/wrapper hashes, original path metadata, module transitions, successful subprocess exits and the import-only path equality. For component observations preserve payload/adapter/runtime envelope checks, identical first/repeated formatted text, private output byte equality and unchanged adapter bytes before/after. Require minimal and probe payload/formatted results to agree between natural and preloaded scenarios on the same runtime/source.

Report import-only medians, natural/preloaded initialization medians, timed explicit import, import-plus-remaining-initialization per observation and total process medians separately. Compute combined-stage medians from per-observation sums, not sums of medians. Report distributions/first observations and paired round differences; do not present either a cross-scenario subtraction or preloading as an exact causal partition or a delivered optimization. Explicit import can warm shared assemblies/JIT/command discovery and may simply move work earlier. Compare matched cohorts within this revision; previous run medians provide context rather than a same-source baseline. A small residual initialization time plus comparable standalone import time would support the module-loading hypothesis; a substantial residual leaves deferred compilation/setup as separate leads. No result automatically authorizes an adapter change.

### Verification and bounded hosted operation

Add meaningful maintainer regressions for closed report/scenario/mode/round validation, module absent/present transitions, natural/preloaded payload disagreement, invalid timer values, stage aggregation, timeouts/incomplete reports and overwrite refusal. Keep the actual PowerShell AST guard regression under strict mode; hosted validation exercises the wrapper's real mode branches and path/module assertions. Run canonical self-test, repository validator, focused Python command-unknown/command-envelope/discover-explicit and pre/post full doctor. Local PowerShell is explicitly unavailable; report skipped/unavailable checks honestly and require hosted coverage for all actual module/prefix behavior.

Replace the completed component step with the 54-observation module diagnostic in the same ordinary Ubuntu 24.04 validate job. Preserve all runtime pins/installation checksum, the 30-minute job timeout, five-minute diagnostic cap, existing ordinary always() behavior and full 305 cases per adapter. Python and Node remain serial; ordinary PowerShell stays at two workers as a diagnostic accompaniment, not a final configuration. If diagnostics fail, ordinary full coverage still runs. Preserve all earlier completed and cancelled findings; do not rewrite accepted history/evidence.

Approval of this complete revision permits continuous local implementation/testing, commits and non-force push to codex/faster-full-verification including already-saved component findings and plan/routing updates, a template-based draft PR #9 description update, exactly one automatic ordinary full-validation attempt with these 54 diagnostics, read-only metadata/log inspection and local findings/record/plan/current-state persistence. Then pause for one measured revision decision in chat. No extra attempt after failure, readiness series, owner PR review/merge approval, optimization or adapter reopening is inferred. Preserve the original checkout's edits and unrelated unpublished history, frozen contract/adapters, accepted evidence and unchanged candidate/publication/activation facts. Exclude dependency/module installation, sharding/reduced coverage/larger runners, other adapter parallelism, preference/profile/settings/authentication changes, certification/publication dispatch, evidence publication/Actions artifact downloads, activation, merging and main integration.

Success requires all 54 valid source/runtime/scenario-bound observations, correct module/path transitions, identical natural/preloaded payload/formatter results, unchanged adapter bytes and full passing ordinary coverage. Diagnostic success does not waive the ten-minute target or establish PR readiness. After local findings persistence, stop; do not begin another diagnosis or optimization automatically.

## Management isolation implementation progress — 2026-09-15

Approved revision 19 is implemented locally: six cohorts, nine alternating-order rounds, 54 fresh serial processes; explicit import-only control and natural/preloaded minimal/probe observations. Closed report validation checks module transitions, matched payloads, paths, source/runtime/round bindings and per-observation combined import/initialization durations. Diagnostic logs carry each observation separately from governed results.

Canonical local self-test: 141 tests, 139 passed, two genuine PowerShell-unavailable skips, zero failures/bytecode. Actual AST and executable-helper checks remain unavailable locally. Focused Python command-unknown, command-envelope and discover-explicit passed 3/3; repository validation and whitespace checks passed. Pre-doctor was 35/36 solely unavailable PowerShell. No hosted module result is claimed yet. Next: final doctor, authorized commit/non-force push, template update and exactly one automatic ordinary full validation attempt, then measured findings and pause.

## Diagnostic-only startup path prototype — complete revised proposal

### Decision and measured basis

The owner selected an isolated diagnostic prototype before reopening the frozen adapter, answering: "Yes, and In future runs we should cancel validation and only read diagnostics when we aren’t testing that changes impact the validation performance and only taking measurements." This accepts the prototype direction and diagnostic-only execution policy, not approval of a previously unseen complete replacement plan. Preserve all earlier approval snapshots and findings.

Run 35019844575 attempt 1 is completed/cancelled on head 42c77cdbb94872ca8e32c876716bcee7386ce42f, tested merge 5abaae7b2d08e27f3ed07d964712afee725d3e9d. Its 54 diagnostic observations passed closed validation, source/runtime/round/module/path checks and natural/preloaded payload/formatter equality; the terminal log SHA-256 is e158697182194333f5b1ae5d1f2c72af23d9dbd479fa38635814d2842c4324c9. Median initialization fell from 0.601765/0.575228s to 0.018134/0.017881s for minimal/probe after explicit import, but whole-process medians improved only from 1.125818/1.421205s to 1.020542/1.330589s. Explicit import medians were 0.089s (standalone control 0.095s), empty process 0.179171s. Stage reductions are not end-to-end savings; untimed/reordered costs remain unresolved. The owner explicitly requested cancellation; terminal cancellation is verified. Full ordinary validation was interrupted, not passed, and no total-job improvement or full coverage is claimed.

### Bounded experiment

Keep the registered adapter, contract, package digests, accepted evidence and current candidate untouched. Extend the independent diagnostic wrapper/driver to compare the original initialization prefix with a counterfactual prefix that replaces exactly these two unique top-level expressions in memory:

- `$script:SkillRoot = Split-Path (Split-Path $PSScriptRoot -Parent) -Parent` becomes `$script:SkillRoot = [IO.Path]::GetDirectoryName([IO.Path]::GetDirectoryName($PSScriptRoot))`.
- `$script:ContractRoot = Join-Path $script:SkillRoot 'assets/contract-v1'` becomes `$script:ContractRoot = [IO.Path]::Combine($script:SkillRoot,'assets/contract-v1')`.

Fail closed if either original expression is missing, duplicated or is not the expected top-level AST assignment. Assert the original strict/null-safe dispatch guard and parse both prefixes under the original adapter path; preserve original path/scoping metadata and compare both computed roots against independently computed .NET expectations. Bind raw source, transformed prefix, wrapper, run/attempt and runtime hashes/identities; label transformed execution as diagnostic counterfactual, never registered-adapter conformance. No transformed adapter file or package variant is written. No module preload, autoload preference changes, profiles/settings, module installation or functions that shadow commands.

Use five serial fresh-process cohorts: empty-process baseline, minimal original, minimal .NET startup, real probe original, real probe .NET startup. Nine rounds yield exactly 45 observations: round zero separately reported, eight sample rounds alternating forward/reverse cohort order. Every process retains NoLogo/NoProfile/NonInteractive and the 15-second timeout. Measure source read, transformation where applicable, source/prefix parsing, block creation, initialization, probe work, first/repeated formatter calls, private-file output and complete process wall/CPU separately. Do not assign synthetic zero timings for absent stages. Retain exclusive output and owned-temporary cleanup, finite timer validation, and unchanged raw adapter/wrapper checks. Emit all observations, provenance and summaries to logs separately from governed results.

Require Management absent before initialization in both variants, present after original initialization, absent after .NET startup initialization. Check inventories again after probe work and formatting: later unchanged commands may still load Management. Do not assert the whole adapter avoids the module. Compare roots, payloads, exact formatter text and repeated/private-file bytes across variants. Summarize matched per-round whole-process differences and stage medians; make whole-process behavior the performance signal, not a stage reduction alone. No minimum speedup is invented for this measurement checkpoint.

### Verification and diagnostic-only hosted execution

Add meaningful maintainer regressions for unique/top-level transform guards, unsupported source/path rejection, transformed-prefix provenance, module transitions by stage, variant payload/formatter mismatch, 45-observation round ordering, timer validation, timeout/crash/incomplete report and overwrite refusal. Exercise actual strict AST/transform/module/path branches on hosted PowerShell; local PowerShell remains explicitly unavailable. Run canonical maintainer self-test, repository validation, focused Python command-unknown/command-envelope/discover-explicit and pre/post full doctors. Distinguish genuine unavailable skips from passes; no fresh full local adapter suites are required for an independent measurement-only wrapper change.

Replace the completed module diagnostic with this 45-observation prototype step in the existing ordinary Ubuntu24.04 validation job, retaining pinned runtimes/checksum, 30-minute job timeout, five-minute diagnostic cap, 15-second invocation timeout, full ordinary validation definition and its always() behavior. Do not change registered-adapter case counts, worker selection, existing conformance result schemas or ordinary verification behavior.

This is explicitly a diagnostic-only attempt. After the diagnostic step completes (success or failure), request force cancellation of that exact run to stop the always() validation step, verify completed/cancelled status, and read terminal metadata/logs. Use bounded read-only checks to recognize diagnostic completion; do not wait for full validation or intentionally collect full-suite timing. Ordinary validation may begin before cancellation arrives; mark all interrupted/pending coverage unavailable and never treat cancellation as failure of conformance or as a full validation pass. If cancellation fails or status remains uncertain, report the exact state and stop without launching another attempt. No additional push/rerun is authorized after diagnostic failure.

Persist the owner's future-run policy in maintainer guidance: identify each authorized hosted run as measurement-only or correctness/full-validation-performance before execution. For measurement-only runs collect diagnostics and cancel remaining ordinary validation; for runs testing changes' correctness or complete validation performance retain required full coverage/timing. Cancellation never waives coverage at PR readiness or replaces the original three consecutive complete under-ten-minute acceptance criterion. This applies to this performance investigation and does not alter certification/publication workflow governance or authorize cancelling unrelated runs.

### Authority, delivery and final stop

Approval of this complete revised plan permits continuous independent wrapper/driver/regression/workflow/documentation implementation, local tests and required approval/progress record/current-state persistence, commits and non-force push to codex/faster-full-verification including saved findings, template-based draft PR #9 update, exactly one automatically triggered diagnostic-only hosted attempt, its exact-run cancellation after diagnostics, read-only logs and local findings/records/plan/routing persistence. Preserve original checkout edits and unrelated unpublished history separately. One PR against candidate-revision-9-certification; no dependent stack or merge group is introduced.

Exclude registered-adapter/contract/governed package changes or candidate reopening/advancement, dependencies/module installation, preference/profile/settings/authentication changes, other adapter parallelism, sharding/reduced coverage/larger runners, certification/publication dispatch, evidence publication or Actions artifact downloads, accepted evidence rewriting, activation, merging or main integration. Two ordinary PowerShell workers remain a diagnostic accompaniment, not final selection. No correctness/full-validation benchmark is authorized in this tranche, and neither whole-adapter module avoidance nor ten-minute readiness is claimed.

Success means all 45 provenance-bound observations and paired results validated, unchanged frozen bytes, real hosted transform/path/module assertions exercised, and exact diagnostic-only cancellation verified. Diagnostic failure still ends this attempt and requires revision, not automatic retry. After findings persistence pause for one measured revision decision in chat. A frozen-adapter change and its required candidate/digest/evidence work require a separately complete approved plan; do not begin them automatically. Draft PR #9 remains not ready for owner review or merging.

## Startup-path prototype implementation progress

The independent wrapper/driver and seven new regressions are implemented. Canonical local self-test passed 145/148 with three genuine PowerShell-unavailable skips, zero failures and bytecode. Repository validator and whitespace checks passed. Actual hosted transform/path/module coverage remains pending; local PowerShell is unavailable. Next is the authorized commit/non-force push, template-based PR update and one diagnostic-only attempt, cancelled immediately after diagnostics finish, followed by local findings and measured review stop.

## Completed startup-path diagnostic findings — 2026-09-15

Diagnostic-only run 35021476856 attempt 1 on head 9aef6de8851290e94ff16d77bf54e5245dbacea4, tested merge 2c9a83a4b568243c44c9ce40e3537660b2b76500, completed all 45 valid observations. Terminal logs were independently validated against exact closed schemas, hashes, runtime7.6.6, source/variant/round bindings, original-path roots, raw/transformed prefix hashes, module transitions and identical paired payload/formatter/private-file bytes. Adapter bytes remained b7f8687b5b4ede2bd124999c23aaa12681a07bddc0597255873fa9c4493fa8c9; wrapper hash cad604a00a7ca348ec86963eaf7e822d2153d7bcf94a43ce86f29101ad2be433. Terminal log SHA-256 9c2617dde0f1f47e49106cf8438d0d906c97f7d443ce177787c591ffea68cdb2. Diagnostic step completed/success.

| Median stage | Original startup | .NET startup |
| --- | ---: | ---: |
| Minimal initialization | 0.586947s | 0.017022s |
| Minimal complete process | 1.147859s | 1.047453s |
| Probe initialization | 0.587983s | 0.015961s |
| Probe work | 0.368969s | 0.888269s |
| Probe complete process | 1.493403s | 1.441741s |

Management was absent after .NET initialization in all 18 variant observations. It remained absent after minimal formatting, but appeared during probe work in all nine .NET probe observations. Original initialization loaded it in all 18 original component observations. The startup change defers module loading for real probe execution; it does not avoid module loading across the whole adapter. Probe stage grew about0.519s while initialization fell about0.572s; complete probe-process median improved only0.052s (about3.5%). Complete minimal median improved0.100s (about8.7%). Individual paired rounds include one slowdown per payload; samples are small. Do not extrapolate stage reduction to 900-invocation/full-job savings or infer the ten-minute target is met. Transformation/guard/report instrumentation is outside ordinary adapter execution and complicates end-to-end attribution.

The ordinary validator finished/failure immediately after diagnostics with `FAIL Python bytecode cache is present`, before diagnostic completion was observed for cancellation. The new independent driver imports its existing component-validation helper through importlib without disabling bytecode; canonical self-test sets no-bytecode controls, so local tests did not expose this standalone diagnostic side effect. This is a harness defect, not evidence of adapter conformance failure. No doctor/self-test/adapter suite/full performance result is claimed for this hosted run. It was already completed/failure when cancellation eligibility was inspected; no cancellation request was sent to a completed run and cancelled status is not claimed. Cancellation acceptance criterion was not met. No additional attempt or corrective push is authorized after this one attempt.

Local pre-delivery validation passed; canonical self-test145/148 with three genuine PowerShell-unavailable skips and no failures/bytecode, focused Python3/3; pre/post doctors35/36 solely unavailable local PowerShell. Actual diagnostic transform/module/path branches are now hosted-verified; the actual transform AST regression in canonical hosted self-test was not reached. Findings remain local to avoid another automatic attempt. Draft PR#9 remains not ready for owner review/merging; full under-ten-minute acceptance remains unmet. Stop for a measured revision decision. A follow-up may address the helper's standalone no-bytecode control and investigate unchanged probe/package calls that load Management, but neither another attempt nor a frozen-adapter change is inferred.

## Isolated probe/package module-avoidance experiment — complete revised proposal

### Purpose and scope

The owner answered "Yes" to preparing a next isolated experiment targeting probe/package module-loading calls and the diagnostic helper fix. This authorizes preparation; explicit approval of this complete replacement remains required before implementation or another attempt. Retain all prior approval snapshots and completed/cancelled findings.

Previous diagnostic-only run35021476856 completed45 valid observations on head9aef6de8851290e94ff16d77bf54e5245dbacea4, tested merge2c9a83a4b568243c44c9ce40e3537660b2b76500. Startup .NET paths deferred Management loading into probe: initialization0.588→0.016s, probe work0.369→0.888s, whole probe1.493→1.442s. Ordinary validator failed on bytecode created by the diagnostic's importlib helper import before cancellation was eligible; full conformance and cancellation were not achieved. The new scope addresses the standalone side effect and distinguishes path-command loading from recursive enumeration cost. It does not reopen the candidate.

### Independent counterfactual variants

Extend the existing independent startup-path wrapper/driver. Never write a transformed adapter, shadow PowerShell commands, alter autoload preferences, or change the registered adapter, contract/package digests or accepted evidence. Keep original path/scoping metadata, strict/null-safe dispatch guards and the two previously approved unique top-level .NET startup replacements.

Use five fresh-process serial measurement cohorts: empty-process baseline; frozen original startup/probe; .NET startup only with original probe; .NET startup plus all seven Join-Path calls within Invoke-WfProbe replaced with IO.Path.Combine; .NET startup plus those seven path replacements and the one governed-scope Get-ChildItem -LiteralPath $path -Recurse -File replaced with an independent diagnostic .NET enumeration helper. Nine rounds (separate first round, eight sample rounds alternating forward/reverse order) produce exactly45 timed observations. No minimal cohorts or full adapter suites are measured here.

Require one unique top-level Invoke-WfProbe function, exactly seven matching Join-Path CommandAst nodes and one exact governed-scope enumeration CommandAst, with original argument shapes and source extents verified. Replace only those command extents inside that function, applying replacements from descending source offset and checking raw/transformed prefix hashes, parse validity, helper-name noncollision and original filename metadata. Unknown/duplicate/nested/changed source shapes fail closed. Preserve all package schemas, registered-adapter/resource digest verification, UTF-8/LF profile checks, governed-scope completeness checks and all nine known answers; do not omit work to improve timings. All other adapter functions remain unchanged in memory.

The helper returns file objects exposing FullName, using System.IO DirectoryInfo/FileInfo/FileSystemInfo facilities. Preserve literal paths, files-only output, hidden/system filtering without Force, inclusion of visible file symlinks according to pinned runtime behavior, and no recursion into directory symlinks/reparse points. Use explicit top-level traversal and inspect attributes/LinkTarget instead of blindly using recursive EnumerateFiles. Fail on unsupported/error behavior rather than silently skipping unreadable entries. This is a Linux/pinned-runtime diagnostic implementation, not a proposed portable adapter replacement.

### Compatibility preflight and instrumentation

Before timing cohorts, execute exactly one fresh untimed compatibility-control process (15-second cap) using the independent helper and original Get-ChildItem as an oracle. Compare normalized ordinal path sets and file type/link classification on each real recursive governed scope and on owned temporary fixtures: empty/nested directories, dot/hidden entries and hidden subdirectories, literal wildcard/bracket names, non-ASCII paths, visible file symlinks, directory symlinks, broken links and a link cycle. Targets stay within owned fixtures; no external links are followed. Compare strict failure behavior on missing roots and a permissions fixture where the runner can enforce denial; an unenforceable denial is explicitly unavailable, not passed. No existing package file is modified. Clean only owned fixtures through .NET operations. Emit closed compatibility report with source/helper/runtime/run bindings and exact scope/fixture coverage; incompatibility ends this attempt before timing.

Total expected process count is46: one untimed compatibility control plus45 timed observations. This explicit control may load Management; it is isolated from every measurement process. It is not counted as a timing observation. Hosted real enumeration/transform guards must be exercised by this preflight and the cohorts, independent of whether canonical hosted self-test begins before cancellation.

Each measured report binds original adapter, wrapper/helper, transformed prefix, source revision/run/attempt, scenario/variant and pinned PowerShell7.6.6. Retain fresh NoLogo/NoProfile/NonInteractive processes, 15-second timeout, exclusive report output, owned private-file byte checks and unchanged raw adapter/wrapper checks. Measure source read, actual transformation where applicable, full/prefix parsing, block creation, initialization, probe work, first/repeated formatting, private-file output and process wall/child CPU separately. No fake zero stages for absent operations. Record module inventories before loading, after initialization, after probe and after formatting; require Management absent through formatting in the full .NET probe variant, but allow and report loading in partial variants where unchanged enumeration remains. All original/partial/full variants must produce identical probe payloads and exact formatter/repeated/private-file bytes. Base performance interpretation on matched per-round whole-process differences; stage reductions alone are insufficient. Preserve complete package work and avoid claiming full-job savings from a small diagnostic sample.

### Standalone helper fix and local verification

Disable Python bytecode writing within the diagnostic process before loading helper modules through importlib; do not change user/environment preferences or repository hooks/settings. Add a meaningful regression in a fresh Python subprocess without -B and with inherited PYTHONDONTWRITEBYTECODE removed, exercising standalone diagnostic/helper import and verifying no repository bytecode is created. Inspect subprocess exit and before/after cache inventory, not just a source-text assertion. Retain canonical self-test bytecode safeguards.

Add regressions for exact AST extent/argument/count/uniqueness guards, prefix/helper provenance, enum compatibility reports and mismatch rejection, module transition differences, identical paired outputs,45 timing/one control coverage and ordering, finite timers, subprocess timeout/crash/incomplete report and overwrite refusal. Run canonical self-test, repository validator, focused Python command-unknown/command-envelope/discover-explicit, whitespace and pre/post full doctor. Local PowerShell remains explicitly unavailable; actual PowerShell guard/helper/preflight coverage is unavailable locally and mandatory hosted. Report unavailable permissions coverage honestly. This diagnostic scope does not require fresh full local adapter suites or a full hosted correctness/performance run.

### One diagnostic-only hosted attempt and delivery authority

Replace the completed startup diagnostic in the existing Ubuntu24.04 validation job with this probe/package diagnostic. Retain runtime pins/install checksum,30-minute job timeout,five-minute diagnostic cap,15-second invocation cap, existing ordinary full305-case definition and always() behavior. Python/Node remain serial and ordinary PowerShelljobs2, with no final count selected. Do not change certification/publication workflows or conformance result schemas.

Approval of this complete revision permits continuous independent wrapper/driver/helper/regression/workflow/docs implementation, required local approval/progress records and current-state routing, commits and non-force push to codex/faster-full-verification including locally saved findings, template-based draft PR9 update, exactly one automatic diagnostic-only hosted attempt, bounded read-only checks for diagnostic completion, exact-run force cancellation immediately after diagnostics finish (success or failure), terminal metadata/log inspection and local findings/records/plan/routing persistence. The diagnostic-only policy means no waiting for full validation and no full-suite benchmark in this tranche. Ordinary validation may start before cancellation arrives; interrupted/pending coverage remains unavailable. If the run becomes terminal before cancellation, preserve its actual conclusion and report why cancellation was inapplicable; never relabel failure as cancellation. If cancellation is uncertain/fails, report and stop; no additional attempt is inferred. A failure requires a revised decision before corrective push/retry.

One independent draft PR against candidate-revision-9-certification, no new dependency/stack/merge group. Preserve original checkout edits and unrelated unpublished history separately. Exclude frozen adapter/contract/governed package edits, candidate reopening/advancement, evidence rewriting/publication or Actions artifact downloads, certification/publication dispatch, activation, dependencies/module installation, profile/preference/settings/authentication changes, other adapter parallelism, sharding/reduced coverage/larger runners, merging or main integration.

Success requires compatibility preflight passed with exact declared coverage (explicit unenforceable-permission limitation allowed), all45 source/runtime/variant-bound measurements valid, unchanged frozen bytes and package work, identical payload/output, full .NET variant's Management absence through probe/formatting, and terminal cancellation or a factually reconciled already-terminal outcome. No speedup threshold is invented for this measurement checkpoint. The original ten-minute target and three consecutive complete passing validation runs remain unmet; diagnostic-only cancellation never waives readiness coverage. After local findings persistence stop for one measured revision decision in chat. No registered-adapter change, new experiment, retry or full validation benchmark begins automatically. Draft PR9 remains not ready for owner review/merging.

Research references for the compatibility boundary: Microsoft Get-ChildItem documentation (https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.management/get-childitem?view=powershell-7.4) and DirectoryInfo API (https://learn.microsoft.com/en-us/dotnet/api/system.io.directoryinfo?view=netframework-4.8.1). Documentation motivates the helper design; pinned hosted compatibility checks establish this diagnostic's behavior, not general cross-platform equivalence.
